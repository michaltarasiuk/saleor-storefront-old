mutation forgotPassword(
  $email: String!
  $redirectUrl: String!
  $channel: String!
) {
  forgotPassword: requestPasswordReset(
    email: $email
    redirectUrl: $redirectUrl
    channel: $channel
  ) {
    errors {
      ...AccountError
    }
  }
}
